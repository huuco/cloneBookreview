<div class="col-md-4">
  <% image  = user_book.book.image.present? ? user_book.book.image.thumb.url : "no_image.png" %>
  <%= image_tag(image, class: "image-book") %>
  <h5><%= link_to user_book.book.name, user_book.book %></h5>
</div>
