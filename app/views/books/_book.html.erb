<div class="row border_book">
  <center>
    <% image = book.image.present? ? book.image.thumb.url : "no_image.png" %>
      <%= link_to image_tag(image, class: "image-cat-book",
      title: book.name), book %>
      <h4><%= link_to book.name, book %></h4>
  </center>
</div>
